{
  "name": "baseflow",
  "version": "0.0.1",
  "private": true,
  "engines": {
    "node": ">=22.12"
  },
  "workspaces": [
    "web",
    "server"
  ],
  "scripts": {
    "format": "biome format",
    "lint": "biome lint",
    "check": "biome check",
    "web:dev": "npm run dev -w web",
    "sev:dev": "npm run dev -w server",
    "dev": "run-p web:dev sev:dev",
    "build": "vite build",
    "serve": "vite preview"
  },
  "dependencies": {},
  "devDependencies": {
    "@biomejs/biome": "^2.3.8",
    "@types/mockjs": "^1.0.10",
    "@types/node": "^22.12.0",
    "cross-env": "^10.1.0",
    "husky": "^9.0.0",
    "lint-staged": "^15.0.0",
    "mockjs": "^1.1.0",
    "npm-run-all": "^4.1.5",
    "replace-in-file": "^8.4.0",
    "rimraf": "^6.1.2",
    "typescript": "^5.7.2"
  }
}
